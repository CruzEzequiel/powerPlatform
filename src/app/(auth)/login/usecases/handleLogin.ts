export default async function handleLogin(email: string, password: string): Promise<boolean> {
    // Simulación de login. Aquí va la lógica real.
    return email === "admin@example.com" && password === "123456";
}
